# Fija los finales de linea para que git no muestre archivos enteros como
# "modificados" al abrirlos en equipos distintos.
# Guardados como LF en el repo, entregados como CRLF en Windows.
*.html text eol=crlf
*.md   text eol=crlf
*.json text eol=crlf
